[package]
name = "banana-backtest"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
banana-core = { workspace = true }
banana-store = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
alloy-primitives = { workspace = true }

[dev-dependencies]
# The point-in-time regression drives the real feature builder rather than a copy of it:
# a test that reimplements the thing it is testing proves nothing.
banana-indexer = { workspace = true }
